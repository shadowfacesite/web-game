{
  "name": "zaton",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "validate": "node tools/validate.mjs",
    "map": "node tools/validate.mjs --map",
    "plan": "node tools/plan.mjs",
    "prebuild": "npm run validate && npm run plan",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "three": "^0.185.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^7.0.0"
  }
}