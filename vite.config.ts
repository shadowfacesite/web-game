import { defineConfig } from "vite";

// Никаких абсолютных путей — грабли 31. base: './' означает, что собранная
// игра запустится и с домена, и из подпапки, и просто из файла на диске.
export default defineConfig({
  base: "./",
  server: {
    host: true, // чтобы открыть с телефона по адресу вида http://192.168.x.x:5173
    port: 5173,
  },
  build: {
    target: "es2022",
    sourcemap: true, // без карт исходников консоль браузера бесполезна для меня
  },
});
